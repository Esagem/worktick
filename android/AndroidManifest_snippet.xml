<!-- AndroidManifest.xml — relevant additions inside <manifest> and <application> -->

<!-- At <manifest> root: -->
<uses-permission android:name="android.permission.INTERNET" />
<!-- For 1-second alarms on Android 12+: -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

<!-- The app block: -->
<application
    android:name=".WorkTickApp"
    ...>

    <receiver
        android:name=".MoneyTickerWidgetProvider"
        android:exported="true">
        <intent-filter>
            <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            <action android:name="dev.surge.worktick.TICK" />
            <action android:name="dev.surge.worktick.REFRESH" />
        </intent-filter>
        <meta-data
            android:name="android.appwidget.provider"
            android:resource="@xml/worktick_money_info" />
    </receiver>

</application>
