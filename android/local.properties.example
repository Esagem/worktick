# local.properties — per-developer config. NEVER commit the real file.
# Copy to local.properties and fill in real values.

sdk.dir=/path/to/your/Android/Sdk

# Google OAuth Client ID. Register the package dev.surge.worktick with your
# debug + release SHA-1 fingerprints at:
#   https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_ID=000000000000-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
